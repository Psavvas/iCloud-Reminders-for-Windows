{
  "name": "reminders-sync",
  "version": "0.2.0",
  "private": true,
  "description": "iCloud Reminders for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/check-sidecar.mjs && node scripts/ui.mjs run build && tauri build",
    "ui": "node scripts/ui.mjs run dev",
    "tauri": "tauri",
    "postinstall": "node scripts/ui.mjs install"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
