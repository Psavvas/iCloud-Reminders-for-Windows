{
  "name": "reminders-sync",
  "version": "0.1.0",
  "private": true,
  "description": "iCloud Reminders for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/check-sidecar.mjs && tauri build",
    "ui": "npm --prefix src-react run dev",
    "tauri": "tauri",
    "postinstall": "npm --prefix src-react install"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
