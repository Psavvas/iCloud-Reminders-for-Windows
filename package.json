{
  "name": "reminders-sync",
  "version": "0.1.0",
  "private": true,
  "description": "iCloud Reminders for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/check-sidecar.mjs && tauri build",
    "tauri": "tauri",
    "prebuild-check": "node scripts/check-sidecar.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
