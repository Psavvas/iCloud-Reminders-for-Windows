{
  "name": "reminders-sync",
  "version": "0.1.0",
  "private": true,
  "description": "iCloud Reminders for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
